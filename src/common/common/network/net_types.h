#pragma once

#include "../types.h"

// Port, used for uh ports
using port_t = u16;

// Client ID, the number used for peer naming over the network
using peer_id_t = u8;

// Command, the numerical representation of a command sent over a network
using command_t = u16;
