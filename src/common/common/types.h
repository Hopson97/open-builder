#pragma once

#include <cstdint>

using u8 = uint8_t;
using u16 = uint16_t;
using u32 = uint32_t;
using u64 = uint64_t;

using Port = u16;
using ClientId = u8;

using EntityId = u16;

using Command = u8;

using Input = u8;
