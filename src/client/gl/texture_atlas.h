#pragma once

/*
#include "texture2d.h"

#include <SFML/System/Vector2.hpp>
#include <array>

namespace client {
    class TextureAtlas final {
      public:
        TextureAtlas(const std::string &textureFileName, int textureSize);

        std::array<GLfloat, 8> getTexture(const sf::Vector2i &coords);

        void bind() const;

      private:
        Texture2D m_texture;

        int m_imageSize;
        const int m_individualTextureSize;
    };
} // namespace client
*/